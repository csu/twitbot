def process_tweet(tweet_text, mentions, api):
    pass
    # content = '%s %s' % (' '.join(mentions), response)
    # tweet_id = status.id
    # api.update_status(status=content, in_reply_to_status_id=tweet_id)